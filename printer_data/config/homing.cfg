# Note you may need to add the following to your printer.cfg somewhere (without the comments of course) for the Kinematic position stuff below to work.
#[force_move]
#enable_force_move: True

[gcode_macro _HOME_X]
gcode:
    {% set run_x = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set run_y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    {% set home_current = 0.45 %}

    # Reduce current
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={home_current}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={home_current}

    # Home X
    G28.1 X
    G91
    G1 X5 F1800
    M400

     # Move slightly off the endstop
    G91                # Relative moves
    G1 X5 F1800        # Move 5mm away from the endstop
    G90                # Back to absolute positioning
    M400

    # Restore currents
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={run_x}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={run_y}


[gcode_macro _HOME_Y]
gcode:
    {% set run_x = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set run_y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    {% set home_current = 0.45 %}

    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={home_current}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={home_current}

    G28.1 Y
    G91
    G1 Y5 F1800
    M400

     # Move slightly off the endstop
    G91                # Relative moves
    G1 X5 F1800        # Move 5mm away from the endstop
    G90                # Back to absolute positioning
    M400

    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={run_x}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={run_y}


[gcode_macro _HOME_XY]
gcode:
    {% set run_x = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set run_y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    {% set home_current = 0.45 %}

    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={home_current}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={home_current}

    G28.1 X Y
    G91
    G1 X5 Y5 F1800
    M400

    # Move slightly off the endstop
    G91                # Relative moves
    G1 X5 F1800        # Move 5mm away from the endstop
    G90                # Back to absolute positioning
    M400


    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={run_x}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={run_y}


[homing_override]
axes: xyz
gcode:
  {% set home_all = 'X' not in params and 'Y' not in params and 'Z' not in params %}

  # Temporary Z for safe moves
  SET_KINEMATIC_POSITION Z=5

  {% if home_all or 'X' in params %}
    G28.1 X
  {% endif %}

  {% if home_all or 'Y' in params %}
    G28.1 Y
  {% endif %}

  {% if home_all or 'Z' in params %}
    G1 X100 Y100 F6000
    G28.1 Z
    G1 Z10 F1500
  {% endif %}


[gcode_macro G28]
description: Smart CoreXY homing macro using sensorless homing
rename_existing: G28.1
gcode:
    {% set axes = params %}
    {% if 'X' in axes and 'Y' in axes %}
        G28.1 X Y
    {% elif 'X' in axes %}
        G28.1 X
    {% elif 'Y' in axes %}
        G28.1 Y
    {% elif 'Z' in axes %}
        G28.1 Z
    {% else %}
        G28.1 X Y
        G1 X100 Y100 F6000
        G28.1 Z
    {% endif %}
